export interface FeatureType {
    title: string;
    description: string;
    icon: string;
  }
  
  export interface TeamMemberType {
    name: string;
    role: string;
    image: string;
  }
  
  export interface ProcessStepType {
    title: string;
    description: string;
    icon: string;
  }